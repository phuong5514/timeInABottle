<UserControl x:Class="TimeInABottle.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<StackPanel Background="LightGray" Padding="10">
		<TextBlock Text="{Binding TaskName}" FontSize="16" FontWeight="Bold" />
		<TextBlock Text="{Binding TaskTime, StringFormat='hh\\:mm'}" FontSize="14" Foreground="Gray" />
		<TextBlock Text="{Binding TaskDescription}" FontSize="12" Margin="0,10,0,0" />
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="Close" Command="{Binding CloseCommand}" Margin="5,0" />
			<Button Content="Snooze" Command="{Binding SnoozeCommand}" Margin="5,0" />
		</StackPanel>
	</StackPanel>
</UserControl>
